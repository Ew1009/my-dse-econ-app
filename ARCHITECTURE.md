# 🔄 System Architecture Diagram

## Application Flow

```
┌─────────────────────────────────────────────────────────────┐
│                         USER                                 │
│                      (Web Browser)                           │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ Opens index.html
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    index.html                                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Loads in order:                                      │   │
│  │  1. questions.js → Question Bank (500+ MCQs, 8 LQs)  │   │
│  │  2. config.js    → AI Configuration                  │   │
│  │  3. app.js       → Core Logic                        │   │
│  │  4. app-*.js     → Feature Modules                   │   │
│  │  5. style.css    → Styling                           │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ User navigates
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                   Main App (app.js)                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  - Navigation (go function)                           │   │
│  │  - State Management (S object)                        │   │
│  │  - Utilities (toast, Modal, renderMd)                │   │
│  │  - LocalStorage persistence                           │   │
│  └──────────────────────────────────────────────────────┘   │
└─────┬─────────────┬──────────────┬───────────────┬──────────┘
      │             │              │               │
      ▼             ▼              ▼               ▼
  ┌──────┐    ┌──────────┐   ┌─────────┐    ┌────────────┐
  │ MCQ  │    │  Long Q  │   │Analytics│    │ AI Features│
  └──┬───┘    └────┬─────┘   └────┬────┘    └─────┬──────┘
     │             │              │               │
     ▼             ▼              ▼               ▼
```

---

## Feature Modules

### 1. MCQ Practice Module
```
┌─────────────────────────────────────────────┐
│         app-mcq.js (Landing)                │
│  - Topic selection                          │
│  - Mode picker (Topic/Exam/Quiz)            │
│  - Filters & settings                       │
└────────────────┬────────────────────────────┘
                 │ User starts quiz
                 ▼
┌─────────────────────────────────────────────┐
│      app-mcq-session.js (Active Quiz)       │
│  - Question display                         │
│  - Answer selection                         │
│  - Instant feedback (topic mode)            │
│  - Scoring & results                        │
└────────────────┬────────────────────────────┘
                 │ Session complete
                 ▼
       Saves to S.mcq.history
       Updates S.stats
```

### 2. Long Questions Module
```
┌─────────────────────────────────────────────┐
│        app-longq.js (Question Bank)         │
│  - Browse questions                         │
│  - Search & filters                         │
│  - Question cards                           │
└────────────────┬────────────────────────────┘
                 │ User selects question
                 ▼
┌─────────────────────────────────────────────┐
│    app-longq-session.js (Answer Session)    │
│  - Rich text editor                         │
│  - Graph drawing tab                        │
│  - Progress tracking                        │
│  - Part navigation                          │
└────────────────┬────────────────────────────┘
                 │ User submits
                 ▼
┌─────────────────────────────────────────────┐
│         app-ai.js (AI Feedback)             │
│  - Build feedback prompt                    │
│  - Call AI API                              │
│  - Parse & display feedback                 │
└────────────────┬────────────────────────────┘
                 │ Feedback received
                 ▼
       Saves to S.longQ.history
       Updates S.stats
```

### 3. AI Features Module
```
┌─────────────────────────────────────────────┐
│           app-ai.js (AI Hub)                │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  1. AI Question Generation          │   │
│  │     - Type selection (MCQ/Long Q)   │   │
│  │     - Parameter setup               │   │
│  │     - Generate button               │   │
│  │     - Display results               │   │
│  └──────────────┬──────────────────────┘   │
│                 │                           │
│  ┌──────────────▼──────────────────────┐   │
│  │  2. AI Tutor Chat                   │   │
│  │     - Question input                │   │
│  │     - Conversation display          │   │
│  │     - Suggestion chips              │   │
│  │     - Context maintenance           │   │
│  └──────────────┬──────────────────────┘   │
│                 │                           │
│  ┌──────────────▼──────────────────────┐   │
│  │  3. AI Settings                     │   │
│  │     - Provider selection            │   │
│  │     - API key input                 │   │
│  │     - Configuration save            │   │
│  └─────────────────────────────────────┘   │
└─────────────────┬───────────────────────────┘
                  │ All use →
                  ▼
┌─────────────────────────────────────────────┐
│      config.js (AIHelper)                   │
│                                             │
│  callAI(prompt, options)                    │
│      │                                      │
│      ├─ OpenAI API                          │
│      ├─ Anthropic API                       │
│      └─ Gemini API                          │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
        ┌──────────────────┐
        │  AI Provider API │
        │  (OpenAI/        │
        │   Anthropic/     │
        │   Gemini)        │
        └──────────────────┘
```

### 4. Analytics Module
```
┌─────────────────────────────────────────────┐
│       app-analytics.js (Dashboard)          │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Overview Tab                       │   │
│  │  - KPI cards (sessions/accuracy)    │   │
│  │  - Score trend chart                │   │
│  │  - Activity heatmap                 │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Topics Tab                         │   │
│  │  - Radar chart                      │   │
│  │  - Topic accuracy cards             │   │
│  │  - Progress bars                    │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  History Tab                        │   │
│  │  - All sessions listed              │   │
│  │  - Scores & dates                   │   │
│  │  - Performance badges               │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Insights Tab                       │   │
│  │  - Weak topics                      │   │
│  │  - Strong topics                    │   │
│  │  - Study tips                       │   │
│  │  - Doughnut chart                   │   │
│  └─────────────────────────────────────┘   │
└─────────────────┬───────────────────────────┘
                  │ Reads from
                  ▼
              S.stats object
              S.mcq.history
              S.longQ.history
```

---

## Data Flow

### State Management (S object)
```
S = {
  view: 'dashboard',              // Current page
  
  mcq: {
    ses: {...},                   // Active MCQ session
    history: [...]                // Past MCQ sessions
  },
  
  longQ: {
    ses: {...},                   // Active Long Q session
    history: [...],               // Past Long Q sessions
    bank: [...]                   // Long Q bank (from questions.js)
  },
  
  aiGen: {
    history: [...]                // Generated questions history
  },
  
  stats: {
    sessions: 0,                  // Total sessions
    time: 0,                      // Total time studied
    recent: [...],                // Recent activity
    topics: {...},                // Per-topic stats
    graphsUsed: 0                 // Graph tool usage
  }
}

↓ Persisted to

localStorage['dse_econ_data']     // Auto-save
```

### API Key Storage
```
User enters key in AI Settings
         ↓
localStorage.setItem('ai_api_key', key)
localStorage.setItem('ai_provider', 'openai')
         ↓
AIHelper.getApiKey() retrieves it
         ↓
Used in API calls to AI providers
```

---

## Security Architecture

### API Key Protection Layers

```
Layer 1: Default Configuration
┌────────────────────────────────────┐
│  config.js                         │
│  provider: 'user-input'  ← DEFAULT │
│  apiKeys: { all empty }            │
└────────────────────────────────────┘
         ↓
    No keys in code!


Layer 2: .gitignore Protection
┌────────────────────────────────────┐
│  .gitignore                        │
│  js/config.js  ← EXCLUDED          │
└────────────────────────────────────┘
         ↓
    Can't commit even if you try!


Layer 3: User Prompt System
┌────────────────────────────────────┐
│  AIHelper.promptForApiKey()        │
│  - Asks user to choose provider    │
│  - Requests API key                │
│  - Saves to localStorage only      │
└────────────────────────────────────┘
         ↓
    Key never leaves user's browser!


Layer 4: localStorage Isolation
┌────────────────────────────────────┐
│  localStorage (Browser-specific)   │
│  - Not transmitted to server       │
│  - Not visible to other sites      │
│  - Stays on user's device          │
└────────────────────────────────────┘
```

---

## API Call Flow

### Example: AI Feedback for Long Question

```
1. USER SUBMITS ANSWER
   ↓
   submitLongQ(container)
   
2. BUILD PROMPT
   ↓
   Combine:
   - Question text
   - Student answers
   - Grading instructions
   
3. CHECK API KEY
   ↓
   AIHelper.isConfigured()
   - If no key → Prompt user
   - If key exists → Continue
   
4. CALL AI
   ↓
   AIHelper.callAI(prompt, options)
   
5. ROUTE TO PROVIDER
   ↓
   if (provider === 'openai')
     → _callOpenAI()
   else if (provider === 'anthropic')
     → _callAnthropic()
   else if (provider === 'gemini')
     → _callGemini()
   
6. MAKE HTTP REQUEST
   ↓
   fetch(apiEndpoint, {
     headers: { Authorization: 'Bearer ' + apiKey },
     body: JSON.stringify({ prompt, options })
   })
   
7. RECEIVE RESPONSE
   ↓
   Parse JSON response
   Extract text content
   
8. DISPLAY TO USER
   ↓
   renderMd(response)
   Show in feedback area
   Save to history
```

---

## File Dependencies

```
index.html
    ├─→ css/style.css
    │
    ├─→ js/questions.js (no dependencies)
    │
    ├─→ js/config.js (no dependencies)
    │
    ├─→ js/app.js
    │   └─→ Uses: questions.js, config.js
    │
    ├─→ js/app-mcq.js
    │   └─→ Uses: app.js (Sections, S, topicById)
    │
    ├─→ js/app-practice.js
    │   └─→ Uses: app.js, app-mcq.js
    │
    ├─→ js/app-mcq-session.js
    │   └─→ Uses: app.js, app-mcq.js
    │
    ├─→ js/app-longq.js
    │   └─→ Uses: app.js, questions.js
    │
    ├─→ js/app-graph.js
    │   └─→ Uses: app.js (GraphTool global)
    │
    ├─→ js/app-analytics.js
    │   └─→ Uses: app.js (Sections, S, Chart.js)
    │
    ├─→ js/app-longq-session.js
    │   └─→ Uses: app.js, app-longq.js, app-graph.js
    │
    └─→ js/app-ai.js
        └─→ Uses: app.js, config.js, questions.js

⚠️ ORDER MATTERS! Load in sequence shown above.
```

---

## External Dependencies (CDN)

```
┌───────────────────────────────────────┐
│  Font Awesome 6.5.1                   │
│  Purpose: Icons                       │
│  CDN: cdnjs.cloudflare.com           │
└───────────────────────────────────────┘

┌───────────────────────────────────────┐
│  Plus Jakarta Sans                    │
│  Purpose: Typography                  │
│  CDN: fonts.googleapis.com           │
└───────────────────────────────────────┘

┌───────────────────────────────────────┐
│  Chart.js 4.4.0                       │
│  Purpose: Analytics charts            │
│  CDN: cdn.jsdelivr.net               │
└───────────────────────────────────────┘

┌───────────────────────────────────────┐
│  marked.js                            │
│  Purpose: Markdown rendering          │
│  CDN: cdn.jsdelivr.net               │
└───────────────────────────────────────┘

┌───────────────────────────────────────┐
│  DOMPurify                            │
│  Purpose: HTML sanitization           │
│  CDN: cdnjs.cloudflare.com           │
└───────────────────────────────────────┘
```

---

## Summary

**Data Flow**: User → App → Feature Modules → AI APIs → Response → Display

**Security**: Multi-layer protection with user-input mode, .gitignore, localStorage

**Architecture**: Modular design with clear separation of concerns

**Dependencies**: Minimal external dependencies, all via CDN

---

**This architecture allows:**
✅ Easy maintenance
✅ Secure API key handling
✅ Scalable feature additions
✅ GitHub Pages deployment
✅ Offline-capable (after first load)
